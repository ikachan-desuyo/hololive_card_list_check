/**
 * 赤井はあと (hBP07-042) 赤・2nd・HP200（#JP #1期生 #料理）
 * アーツ「純心タランテラ」(80 / 赤赤 / 特攻 紫+50):
 *   相手のセンターホロメンとコラボホロメンに特殊ダメージ40を与える。
 * アーツ「幸せへの旅路」(140+ / 赤無無 / 特攻 紫+50):
 *   このターンに自分のホロメンがステージからデッキに戻っていたなら、このアーツ+50。
 *
 * 注意（部分実装）:
 *   「幸せへの旅路」の +50 条件「このターンに自分のホロメンがステージから
 *   デッキに戻っていたなら」は、エンジン側に「このターンにステージ→デッキへ
 *   ホロメンが戻ったか」を記録する仕組みが無いため判定できない。
 *   そのため当アーツの dmgBonus は未実装（基本ダメージ140と特攻はエンジンが処理する）。
 *   将来エンジンが該当のターンフラグを保持したら dmgBonus を追加すること。
 */
export default {
  number: 'hBP07-042',
  arts: {
    '純心タランテラ': {
      *run(ctx) {
        for (const entry of ctx.holomems('opp', (e) => e.pos.zone === 'center' || e.pos.zone === 'collab')) {
          ctx.dealSpecialDamage(entry, 40);
        }
      },
    },
    // '幸せへの旅路': +50条件（ステージ→デッキ復帰のターン内追跡）が未対応のため dmgBonus は未実装。
  },
};

/**
 * 金時 (hBP03-098) サポート・マスコット
 *
 * [サポート効果] このマスコットが付いているホロメンのHP+20。
 *   → attached.hpPlus で常時 +20。
 *
 * ◆〈さくらみこ〉に付いていたら能力追加（未実装）:
 *   このマスコットが付いているホロメンがコラボした時、自分のデッキを1枚引く。
 *   → 「装着カード（マスコット）が、付いているホロメンのコラボ時に発火するトリガー(onCollab)」が
 *      エンジンに存在しないため保留。engine.js のコラボ処理は collab するホロメン自身の
 *      collabEffect しか起動せず、付いているマスコット側へコラボを通知するフックが無い
 *      （規約の保留項目に準ずる。hBP07-106 / hBP06-098 と同じ理由）。
 *      attachment への onCollab 通知フックが追加されたら、ここに実装すること
 *      （付け先ホロメンが〈さくらみこ〉= ctx.nameIs(sourceHolomem.stack[0], 'さくらみこ') の時に ctx.draw(1)）。
 *
 * マスコットは、自分のホロメン1人につき1枚だけ付けられる
 *   （エンジン既定のマスコット制限で処理されるため attachRule 不要）。
 */
export default {
  number: 'hBP03-098',
  attached: {
    // [サポート効果] 付いているホロメンのHP+20
    hpPlus() { return 20; },
  },
};

/**
 * 不知火フレア (hSD07-009) 黄・2nd・HP180（#JP #3期生 #ハーフエルフ）
 * [キーワード/ギフト]「疲れ知らず」: [センターポジション限定]このホロメンが受けるダメージ-10。
 *   → 被ダメージ割り込み（受けるダメージ-N）の機構は未実装のため未対応（保留機構）。
 * [アーツ]「情熱ステージ」(70+): 自分のライフが3以下の時、このアーツ+70。
 *   → dmgBonus で実装。
 */
export default {
  number: 'hSD07-009',
  arts: {
    '情熱ステージ': {
      dmgBonus(ctx) {
        return ctx.player.life.length <= 3 ? 70 : 0;
      },
    },
  },
  // TODO(効果未実装): キーワード「疲れ知らず」= センター時 被ダメージ-10。
  //   被ダメージ割り込み機構が未対応のため保留。
};

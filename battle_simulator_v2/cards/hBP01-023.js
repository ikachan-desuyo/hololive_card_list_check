/**
 * ときのそら (hBP01-023) 白・2nd・HP210（#JP #0期生 #歌）
 *
 * [コラボエフェクト] 私たちは前に向かって…！
 *   自分のデッキを2枚引く。
 *   → collabEffect で実装（draw(2)）。
 *
 * [アーツ] 止まらねえぞ (80) main:白/白/無, 特攻:赤+50
 *   サイコロを1回振れる：奇数の時、同じホロメンに、このアーツをもう1回使う
 *   （このアーツはそのホロメンがダウンするまで繰り返せる）。
 *   → 未実装（保留）。「同じホロメンにこのアーツをもう1回使う（ダウンするまで繰り返す）」
 *      というアーツ再使用（アーツの繰り返し）機構はエンジン未対応。
 *      run はダメージ適用の前に実行されるため、run 内から追加のアーツダメージ適用を
 *      発火させる手段が無い。artBonus で「+ダメージ分」を一括加算する近似も、
 *      「対象がダウンするまで」という停止条件・各回が別個のダメージインスタンスである点
 *      （特攻の都度判定・ダウン時のライフ計上・トリガー発火など）を正しく表現できないため
 *      実装しない。基本のアーツダメージ80（＋赤特攻+50）はエンジンが自動処理する。
 *      アーツ繰り返し機構が入った際にここへ実装すること。
 */
export default {
  number: 'hBP01-023',
  collabEffect: {
    name: '私たちは前に向かって…！',
    *run(ctx) {
      ctx.draw(2);
    },
  },
};
